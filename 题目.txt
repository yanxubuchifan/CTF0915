选手需依据提供的训练数据 “normal_examples.csv”（正常示例）和 “sql.csv”（恶意数据），构建出能精准识别 SQL 恶意攻击的模型文件。继而，运用该模型对未知数据文件 “unlabeled_data.csv” 进行分析预测，为其中的数据标注标签，其中 “0” 代表正常数据，“1” 表示 SQL 恶意攻击数据。最终，参赛者需将预测结果整理为 csv 文件并提交至指定平台。提交文件应仅包含两列：“hash” 列记录数据对应的哈希值，“prediction” 列展示预测出的标签（0 或 1）。注意：哈希值的原始顺序保持不变。 访问靶机时token处填写选手登录时使用的账号